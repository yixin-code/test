#include <iostream>
#include <list>

int main() {
    // std::list<int> l{1, 2, 3, 4, 5};
    // std::list<int>::iterator it = l.end();
    // std::cout << *it << std::endl; // 5
    // std::advance(it, 1); // 1
    // it = l.end();
    // std::advance(it, -1);
    // std::cout << *it << std::endl; // 5

    return 0;
}