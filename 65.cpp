#include <unistd.h>
#include <iostream>
#include <fstream>
// #include "66.cpp"

int main(int argc, char *argv[]) {
    func();
    // std::fstream fio("65.txt", std::ios::in | std::ios::out);
    // std::fstream fio("65.txt", std::ios::out | std::ios::in);
    // std::ifstream fin("65.txt");
    // // std::ofstream fout("65.txt", std::ios::w)
    // if (!fin.is_open()) {
    //     perror("fin");
    //     exit(1);
    // }
    // char buf[1024] = {0};
    // std::streamsize size = fin.read(buf, sizeof(buf)).gcount();
    // // fin.getline(buf, sizeof(buf)).gcount();
    // std::cout << size << std::endl;
    // fin.close();
    // std::ofstream fout("65.txt");
    // fout << "123";
    // fout.write("123", 3);

    // char buf[10] = {0};
    // int count = read(STDIN_FILENO, buf, sizeof(buf));
    // write(STDOUT_FILENO, buf, count);
    // int num = 0;
    // std::cout << ~num << std::endl;

    // return 0;
    // exit(-1);
    return 0;
}
