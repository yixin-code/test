#include <iostream>
#include <unistd.h>
#include <string.h>

int main(int argc, char *argv[]) {
    
    // execlp("./78", "./78", nullptr);

    // // std::cout << "79 end\n";
    // std::cout << (long)(void*)-1 << '\n';

    // int num = -1;

    // if (num == (long)(void*)-1) {
    //     std::cout << "num < (void*)-1\n";
    // }

    // void *p = (void*)-1;
    // if ((long)p == -1) {
    //     std::cout << "(int)p == -1\n";
    // }

    // std::cout << sizeof(int64_t) << '\n';
    // std::cout << sizeof(p) << '\n';

    return 0;
}